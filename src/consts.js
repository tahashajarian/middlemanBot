const user_status_enum = {
  available: "available",
  matched: "matched",
  idle: "idle",
};

const commands_enum = {
  match: "/match",
  disconnect: "/disconnect",
  start: "/start",
  pause: "/pause",
};

module.exports = { user_status_enum, commands_enum };
